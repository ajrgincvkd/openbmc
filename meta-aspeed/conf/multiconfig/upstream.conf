# Configuration for building with community (upstream) versions
# This enables both Linux kernel 6.1 and U-Boot 2024.01 from upstream sources

BOOTLOADER_VARIANT = "upstream"
KERNEL_VARIANT = "upstream"
